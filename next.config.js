/** @type {import('next').NextConfig} */

const nextConfig = {
    output: 'export',
    basePath: '/react-konva-to-svg',
    images: {
        unoptimized: true,
    },
}

module.exports = nextConfig
