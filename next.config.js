/** @type {import('next').NextConfig} */

const nextConfig = {
    output: 'export',
    basePath: '/konva-react-to-svg',
    images: {
        unoptimized: true,
    },
}

module.exports = nextConfig
